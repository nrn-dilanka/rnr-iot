# RabbitMQ MQTT Plugin Configuration
# Map MQTT connections to the iot_vhost virtual host

mqtt.default_user = iotuser
mqtt.default_pass = iotpassword
mqtt.vhost = iot_vhost
mqtt.exchange = amq.topic
mqtt.max_session_expiry_interval_seconds = 86400
mqtt.prefetch = 10
